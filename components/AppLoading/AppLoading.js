import { View, Text } from 'react-native';
import React from 'react';

const AppLoading = () => {
    return (
        <View>
            <Text></Text>
        </View>
    );
};

export default AppLoading;
